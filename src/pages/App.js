import React from 'react';
import AddPersonForm from './person'; // Assuming AddPersonForm is located in the same directory as App.js

const App = () => {
    return (
        <div>
            <h1>Add Person</h1>
            <personForm />
        </div>
    );
};

export default App;
